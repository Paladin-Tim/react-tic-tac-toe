import { GameLayout } from "./GameLayout";

export const Game = () => {
  //const { fields, currentPlayer, isWinner, isDraw } = store.getState();

  return (
    <>
      <GameLayout
      // fields={fields}
      //   setFields={setFields}
      // currentPlayer={currentPlayer}
      //  setCurrentPlayer={setCurrentPlayer}
      // isWinner={isWinner}
      // calculateWinner={calculateWinner}
      // isDraw={isDraw}
      // setIsDraw={setIsDraw}
      />
    </>
  );
};
